import 'package:flutter/material.dart';

class ImageGenTitle extends StatelessWidget {
  const ImageGenTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '图片生成',
    );
  }
}
