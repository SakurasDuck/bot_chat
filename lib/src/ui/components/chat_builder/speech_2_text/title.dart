import 'package:flutter/material.dart';

class Speech2TextTitle extends StatelessWidget {
  const Speech2TextTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '语音转文字',
    );
  }
}
